var bar = {
  getGithubOrgs: function(url) {
    // re = fetch(url).then((response) => response.json());
    return 're'
  }
}

// // function getGithubOrgs(url) {
// //   return fetch(url).then((response) => response.json());
// // }

// bar.getGithubOrgs("https://404mike.github.io/nel_results/data/manifests/62599b02598a4/full-index.json").then((res) =>
//   console.log(res)
// );